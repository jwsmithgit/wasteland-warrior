// gets fraction from min, max, and current value;

var min_value = argument0;
var max_value = argument1;
var current_value = argument2;

return ( current_value - min_value ) / ( max_value - min_value );
